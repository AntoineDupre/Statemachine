#!/usr/bin/env python

# Imports
import os
from setuptools import setup


# Setup
setup(name="python-statemachine",
      version="0.1.0",
      description="State machine used in modulator conditioning and magnet cycling",
      license="GPLv3",
      packages=["statemachine"],
      )
